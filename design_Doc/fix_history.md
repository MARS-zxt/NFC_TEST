# **FIX_1:**

**一、开卡界面 OpenCpuTypeACardDemo.html**

1	密钥输入长度校验，非16个字符需要做提示，比如输入框标红，然后有提示词
2	按钮调整	三个按钮移到独立第4行，文字完整显示
3	卡类型下拉	 → <SELECT></select>，选项"用户卡"(01) / "维护卡"(02)
**二、APDU 界面 TestCpuTypeA.html**

1	优化调整按钮和输入框的位置，按钮大小美观合适能够展示完全文字
2	密钥输入长度校验，非16个字符需要做提示，比如输入框标红，然后有提示词
**三、燃气卡读写界面 LoadCpuTypeACardDemo.html**

1	token 数下拉	 → <SELECT></select>，选项 1~5，默认选中 3

# **FIX_2:**

**一、开卡界面 OpenCpuTypeACardDemo.html**

1、<表号> 如果未输入要做输入提示，提示词：请输入表号，最大长度为20位

2、<用户ID>如果未输入要做输入提示，提示词：请输入用户ID，最大长度为20位

3、如果<初始密钥>、<主控密钥>、<表号>、<用户ID>输入不合法，点击【开卡】按钮不执行对应操作，需要做弹窗提示，提示词需要具体说明那个输入项不合法

合法：

<初始密钥>:长度需为32位

<主控密钥>:长度需为32位

<表号>：不能为空

<用户ID>：不能为空

# FIX_3:

帮我检查这段代码：ii



hzxhz

其中0字节是cardType,

1-21字节是utilityID，其中1字节代表号码长度，根据长度来读取号码，最长20位

22-42字节是userID，其中22字节代表号码长度，根据长度来读取号码，最长20位

# FIX_4:

帮我检查这段读取气表状态文件数据的代码：

0-11字节是`readDate`，代表读取日期

12-15字节是`meterStatus`，代表表状态

16-19字节是`mask_status`，代表状态掩码

20-23字节是`totalGas`，代表累积气量

24-27字节是`creditLimit`，代表信用额度

28-31字节是`balance`，代表余额

32-33字节是`mainVoltage`，代表状态主电池电压
